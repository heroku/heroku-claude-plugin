{
  "name": "heroku-plugin",
  "version": "0.1.0",
  "description": "Scaffold and deploy applications to Heroku from natural language prompts.",
  "author": {
    "name": "jwinn"
  },
  "keywords": ["heroku", "scaffold", "deployment", "docker", "buildpack"],
  "license": "MIT",
  "policy": {
    "reference_staleness_days": 30,
    "deploy_mode": "mcp",
    "mcp_endpoint": "https://mcp-portal-canary.herokai.com/mcp",
    "mcp_auth_env_var": "HEROKAI_SECRET",
    "mcp_stub": false,
    "supported_stacks": ["node", "python", "rails", "go"],
    "supported_addons": ["heroku-postgresql", "heroku-redis"],
    "unsupported_addons": ["kafka"],
    "token_budget_tracking": false,
    "token_budgets": {
      "_description": "Benchmarks from scenario eval runs — starting points to improve on, not hard limits. scaffold-app and deploy-anonymous measured from FastAPI+Postgres run (2026-08-12); build-and-deploy avg ~18500 across all stacks (2026-08-10); other atomic skills not yet individually measured.",
      "preflight": 300,
      "scaffold-app": 8500,
      "deploy-anonymous": 3200,
      "check-deploy-status": 500,
      "generate-access-code": 400,
      "claim-app": 500,
      "go-live": 400,
      "build-and-deploy": 18500
    }
  }
}
