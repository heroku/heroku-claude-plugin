{
  "name": "heroku-plugin",
  "version": "0.1.0",
  "description": "Scaffold and deploy applications to Heroku from natural language prompts.",
  "author": {
    "name": "jwinn"
  },
  "keywords": ["heroku", "scaffold", "deployment", "docker", "buildpack"],
  "license": "MIT",
  "policy": {
    "reference_staleness_days": 30,
    "deploy_mode": "cli",
    "mcp_stub": false,
    "supported_stacks": ["node", "python", "rails", "go"],
    "supported_addons": ["heroku-postgresql", "heroku-redis"],
    "unsupported_addons": ["kafka"],
    "token_budgets": {
      "_description": "Starting estimates — not measured from real Claude invocations. To calibrate: run real skill prompts through Claude, capture actual output token counts, and update these values with headroom. Re-measure when skills change significantly.",
      "preflight": 300,
      "scaffold-app": 800,
      "deploy-anonymous": 600,
      "check-deploy-status": 500,
      "generate-access-code": 400,
      "claim-app": 500,
      "go-live": 400,
      "build-and-deploy": 1200
    }
  }
}
